%CUSIGMOID Sigmoid (logistic) function.
%
%   CUSIGMOID(A,B) calculates 1./(1+exp(-A) for every element of A,
%   storing the result in B. A,B can be a matrix or vector but must be
%   the same size.
%
